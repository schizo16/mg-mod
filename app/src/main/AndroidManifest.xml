<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".MgAfkApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MG AFK"
        android:supportsRtl="true"
        android:theme="@style/Theme.MgAfk">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.MgAfk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- OAuth callback deep link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="mgafk"
                    android:host="oauth" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.OAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.MgAfk" />

        <activity
            android:name=".auth.CasinoOAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.MgAfk" />

        <activity
            android:name=".play.PlayActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:theme="@style/Theme.MgAfk" />

        <activity
            android:name=".service.AlarmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.MgAfk"
            android:launchMode="singleTop"
            android:excludeFromRecents="true" />

        <service
            android:name=".service.AfkService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <receiver
            android:name=".service.AlarmStopReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mgafk.app.STOP_ALARM" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
